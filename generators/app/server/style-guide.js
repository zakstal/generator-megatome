// TODO
module.exports = function(){

	var bower = require('../bower.json').dependencies;

	var components = Object.keys(bower).map(function(componentName){
		// for each component name pull in bower.json
		// check if should be in style-guide
			// return main js
	});

	// append components to style-guide.html

	// return html
};
